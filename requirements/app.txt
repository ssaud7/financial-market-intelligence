# Pipeline runtime. Installed into .venv-app.
# NOTE: torch is installed separately from the PyPI CPU index by
# .devcontainer/post-create.sh — the default Linux wheel drags in ~2.5 GB of
# CUDA libraries this project has no use for.

# ---- Stage 1: ingestion --------------------------------------------------
confluent-kafka==2.5.3
pydantic==2.9.2
pydantic-settings==2.5.2

# ---- Stage 2: Delta lakehouse -------------------------------------------
pyspark==3.5.3
delta-spark==3.2.1

# ---- Stage 3: RAG --------------------------------------------------------
chromadb>=0.5.5,<0.6
sentence-transformers>=3.0,<4.0
rank-bm25==0.2.2
anthropic>=0.40
beautifulsoup4>=4.12
lxml>=5.2
tiktoken>=0.7

# ---- Stage 4: quality gates + lineage -----------------------------------
great-expectations>=1.3,<2
openlineage-python>=1.25,<2

# ---- Shared --------------------------------------------------------------
numpy<2
pandas>=2.1,<3
pyarrow>=15,<18
python-dotenv>=1.0
structlog>=24.1
typer>=0.12
rich>=13.7
tenacity>=8.3

# ---- Dev / evidence ------------------------------------------------------
pytest>=8.2
jupyter>=1.0
nbconvert>=7.16
ipykernel>=6.29
